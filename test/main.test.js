const expect = require('chai').expect
const main = require('../src/main')

describe('main', () => {
  beforeEach(() => {

  })
  afterEach(() => {

  })

  it('Everything is working', () => {
    expect(main()).to.equal(true)
  })
})
